# This is a basic workflow to help you get started with Actions

name: pigeon deploy from github

# Controls when the action will run.
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ develop ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: git checkout
        uses: actions/checkout@v2

      - name: Setup Node.js environment
        uses: actions/setup-node@v2.1.5
        with:
          node-version: '14'
      # Runs a single command using the runners shell
      - name: first shell command
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: seceond shell command
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
      - name: Build Image
        id: build-image
        uses: redhat-actions/buildah-build@v2
        with:
          image: pigeon-tony
          tags: latest ${{ github.sha }}
          dockerfiles: |
            ./Dockerfile


      - name: Push To alibaba registry
        id: push-to-alibaba
        uses: redhat-actions/push-to-registry@v2
        with:
          image: ${{ steps.build-image.outputs.image }}
          tags: ${{ steps.build-image.outputs.tags }}
          registry: registry.cn-beijing.aliyuncs.com/ihealthlabs
          username: ijiankang@ihealthlabs.com.cn
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Print image url
        run: echo "Image pushed to ${{ steps.push-to-alibaba.outputs.registry-paths }}"